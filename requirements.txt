# scikit-learn and joblib are pinned because models/ecg_model.joblib is a pickled
# estimator: a joblib/pickle of a RandomForest is not guaranteed to load across
# scikit-learn minor versions. The committed model was trained on scikit-learn
# 1.8.0 / numpy 2.4.x / joblib 1.5.x — keep these ranges in sync if you retrain.
numpy>=1.23,<3
scipy>=1.9
scikit-learn>=1.8,<1.9
joblib>=1.3
matplotlib>=3.4
wfdb>=3.4
flask>=3.0
flask-cors>=4.0
